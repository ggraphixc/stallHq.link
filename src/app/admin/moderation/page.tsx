import { ModerationClient } from "./ModerationClient";

export const metadata = {
  title: "Moderation | StallHq Admin",
};

export default function AdminModerationPage() {
  return <ModerationClient />;
}
